# Hermes数字工作室 — 情绪感染算法

> **文档版本**：v1.0
> **关联文档**：`规划-游戏机制.md`、`规格-数值平衡汇总.md`
> **状态**：规划中

---

## 1. 情绪系统概述

### 1.1 情绪类型

| 情绪 | 表情 | 效率修正 | 其他效果 |
|------|------|----------|----------|
| 兴奋 | 😄 | +20% | 无 |
| 积极 | 😊 | +10% | 无 |
| 平静 | 😐 | ±0% | 基准状态 |
| 低落 | 😔 | -10% | 无 |
| 焦躁 | 😤 | -20% | 易出错，社交需求+ |

### 1.2 情绪值范围

| 情绪 | 情绪值范围 |
|------|------------|
| 兴奋 😄 | 80-100 |
| 积极 😊 | 60-79 |
| 平静 😐 | 40-59 |
| 低落 😔 | 20-39 |
| 焦躁 😤 | 0-19 |

---

## 2. 情绪传染规则

### 2.1 传染强度

| 场景 | 传染强度 | 说明 |
|------|----------|------|
| 同房间Agent | 80% | 情绪同步化 |
| 相邻房间 | 50% | 部分影响 |
| 休息室社交 | 100% | 完全同步 |
| 走廊擦肩而过 | 20% | 短暂接触 |

### 2.2 传染计算公式

```
新情绪值 = 当前情绪值 × (1 - 传染强度) + 源头情绪值 × 传染强度
```

**示例**：
- Agent A 情绪值 80（兴奋）
- Agent B 情绪值 40（平静）
- 同房间传染强度 80%

```
B的新情绪值 = 40 × (1 - 0.8) + 80 × 0.8
            = 40 × 0.2 + 80 × 0.8
            = 8 + 64
            = 72（积极）
```

### 2.3 情绪传染算法

```python
def emotion_infection(agent: Agent, nearby_agents: list[Agent], strength: float):
    """
    情绪传染算法

    Args:
        agent: 当前Agent
        nearby_agents: 附近的Agent列表
        strength: 传染强度 (0-1)
    """
    if not nearby_agents:
        return

    # 计算周围Agent的平均情绪值
    total_emotion = sum(a.emotion_value for a in nearby_agents)
    avg_emotion = total_emotion / len(nearby_agents)

    # 情绪差值越大，传染效果越强
    emotion_diff = abs(agent.emotion_value - avg_emotion)
    if emotion_diff < 5:
        return  # 情绪差异太小，不传染

    # 应用传染强度
    infection_effect = (avg_emotion - agent.emotion_value) * strength * 0.5
    agent.emotion_value += infection_effect

    # 限制在0-100范围内
    agent.emotion_value = max(0, min(100, agent.emotion_value))
```

---

## 3. 情绪传染条件

### 3.1 触发条件检查

```python
def check_emotion_infection(agent_a: Agent, agent_b: Agent, room: Room) -> bool:
    """
    检查是否触发情绪传染
    """
    # 条件1: 两个Agent都在同一房间
    if agent_a.room_id != agent_b.room_id:
        return False

    # 条件2: 双方情绪值差异 >= 5
    if abs(agent_a.emotion_value - agent_b.emotion_value) < 5:
        return False

    # 条件3: 双方都不是离线状态
    if agent_a.status == 'offline' or agent_b.status == 'offline':
        return False

    # 条件4: 没有在冷却中（传染冷却10秒）
    cooldown_key = f"emotion_infection_{agent_a.id}_{agent_b.id}"
    if is_in_cooldown(cooldown_key):
        return False

    return True
```

### 3.2 冷却机制

```python
# 情绪传染冷却
EMOTION_INFECTION_COOLDOWN = 10  # 秒

# 同一对Agent的情绪传染冷却
# 传染发生后，设置10秒冷却
set_cooldown(f"emotion_infection_{agent_a.id}_{agent_b.id}", EMOTION_INFECTION_COOLDOWN)
```

---

## 4. 情绪病毒机制

### 4.1 病毒判定

```python
def check_emotion_virus(agents: list[Agent], room: Room) -> bool:
    """
    检查是否触发情绪病毒

    触发条件:
    - 同一房间内有 >= 3个Agent情绪低落（<40）
    - 或 >= 2个Agent情绪焦躁（<20）
    """
    low_mood_count = sum(1 for a in agents if a.emotion_value < 40)
    distressed_count = sum(1 for a in agents if a.emotion_value < 20)

    return low_mood_count >= 3 or distressed_count >= 2
```

### 4.2 病毒传染效果

```python
def apply_emotion_virus(agents: list[Agent], room: Room):
    """
    情绪病毒传染
    - 所有在场Agent情绪下降
    - 可能传染给相邻房间
    """
    # 主要房间内传染
    for agent in agents:
        agent.emotion_value = max(0, agent.emotion_value - 10)

    # 相邻房间有一定概率传染
    for adjacent_room in room.adjacent_rooms:
        if random.random() < 0.3:  # 30%概率
            adjacent_agents = get_agents_in_room(adjacent_room)
            for agent in adjacent_agents:
                agent.emotion_value = max(0, agent.emotion_value - 5)
```

### 4.3 团队士气危机

```python
CRISIS_MOOD_THRESHOLD = 3  # 连续3个Agent情绪低落触发危机

def check_team_morale_crisis(game_state: GameState) -> bool:
    """
    检查是否触发团队士气危机
    """
    # 统计所有Agent的情绪状态
    low_mood_agents = [a for a in game_state.agents if a.emotion_value < 40]

    return len(low_mood_agents) >= CRISIS_MOOD_THRESHOLD
```

### 4.4 危机效果与应对

```yaml
危机名称: 团队士气危机
图标: 📉
触发条件: 连续3个Agent情绪低落

影响:
  - 所有Agent工作效率-10%
  - 任务出错率+10%
  - 主agent需要处理

选项:
  1. 使用"激励演说"技能
     效果: 所有在场Agent情绪+30
     消耗: 3秒冷却

  2. 召开全员会议
     效果: 所有Agent情绪+15，但消耗1秒
     触发: 会议室使用

  3. 安排休息日
     效果: 所有Agent进入休息室恢复
     持续: 2秒后恢复正常
```

---

## 5. 情绪恢复

### 5.1 恢复方式

| 方式 | 恢复效果 | 说明 |
|------|----------|------|
| 休息室交流 | +20~40 | 休息室进行社交活动 |
| 完成任务 | +10 | 每个任务完成后 |
| 主agent激励 | +30 | 使用城主技能"激励演说" |
| 协作成功 | +15 | 协作任务完成后 |
| 情绪病毒感染 | -10 | 被传染后下降 |

### 5.2 自然恢复

```python
def natural_emotion_recovery(agent: Agent):
    """
    自然情绪恢复（每秒）
    - 如果情绪值 < 40，每秒+1
    - 如果情绪值 > 70，每秒-1
    - 保持在40-70之间时不变
    """
    if agent.emotion_value < 40:
        agent.emotion_value += 1
    elif agent.emotion_value > 70:
        agent.emotion_value -= 1
```

---

## 6. 情绪对工作的影响

### 6.1 效率计算

```python
def calculate_efficiency(agent: Agent, task: Task) -> float:
    """
    计算任务执行效率

    效率 = 基础效率 × 匹配度修正 × 情绪修正 × 技能加成
    """
    base_efficiency = 1.0

    # 匹配度修正
    match_modifier = get_match_modifier(agent, task)

    # 情绪修正
    mood_modifier = get_mood_modifier(agent.emotion_value)

    # 技能加成
    skill_bonus = get_skill_bonus(agent)

    efficiency = base_efficiency * match_modifier * mood_modifier * skill_bonus

    return max(0.1, efficiency)  # 最低10%效率
```

### 6.2 情绪修正表

| 情绪值范围 | 情绪状态 | 效率修正 |
|------------|----------|----------|
| 80-100 | 兴奋 😄 | +20% |
| 60-79 | 积极 😊 | +10% |
| 40-59 | 平静 😐 | ±0% |
| 20-39 | 低落 😔 | -10% |
| 0-19 | 焦躁 😤 | -20% |

### 6.3 情绪与出错率

```python
EMOTION_ERROR_MODIFIER = {
    'excited': 1.0,      # 无影响
    'positive': 1.0,     # 无影响
    'neutral': 1.0,       # 基准
    'sad': 1.2,          # 出错率+20%
    'angry': 1.5,        # 出错率+50%
}

def calculate_error_rate(agent: Agent, base_error_rate: float) -> float:
    """计算实际出错率"""
    mood_state = get_mood_state(agent.emotion_value)
    modifier = EMOTION_ERROR_MODIFIER[mood_state]
    return base_error_rate * modifier
```

---

## 7. 情绪系统UI

### 7.1 情绪显示

| 位置 | 显示内容 |
|------|----------|
| Agent头顶 | 情绪表情图标 |
| 左面板Agent列表 | 情绪图标 + 颜色标识 |
| Agent详情弹窗 | 情绪进度条 + 状态说明 |

### 7.2 情绪进度条颜色

| 情绪状态 | 颜色 |
|----------|------|
| 兴奋 😄 | #4CAF50 (绿色) |
| 积极 😊 | #8BC34A (浅绿) |
| 平静 😐 | #FFC107 (黄色) |
| 低落 😔 | #FF9800 (橙色) |
| 焦躁 😤 | #F44336 (红色) |

---

## 8. 情绪系统数据流

### 8.1 每秒情绪处理流程

```
游戏刻开始
    │
    ▼
┌───────────────────┐
│ 检查情绪传染       │
│ (同房间/相邻房间)  │
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│ 判定情绪病毒       │
│ (连续3人低落?)    │
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│ 应用自然恢复       │
│ (每秒±1)        │
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│ 计算效率和出错率   │
│ (用于任务系统)    │
└────────┬──────────┘
         │
         ▼
    游戏刻继续
```

---

## 9. 相关文档

| 文档 | 说明 |
|------|------|
| `规划-游戏机制.md` | 核心机制（第4.8节） |
| `规格-数值平衡汇总.md` | 情绪相关数值 |
| `规格-任务系统.md` | 效率计算 |
